<Window x:Class="ToDoApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToDoApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" /> <!-- Task list takes remaining space-->
            <RowDefinition Height="Auto" /> <!--Input area--> 
            <RowDefinition Height="Auto" /> <!--Buttons take only what they need-->
        </Grid.RowDefinitions>
        <!--Row 0: Task list-->
        <ListBox x:Name="TaskList" Grid.Row="0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Title}" FontWeight="Bold" />
                        <TextBlock Text="{Binding DueDate}" FontStyle="Italic" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!--Row 1: InlineInput area (starts hidden)-->
        <StackPanel x:Name="InputPanel" Grid.Row="1" Orientation="Vertical" Margin="5">
            <Label Content="Title" VerticalAlignment="Center"/>
            <TextBox x:Name="TitleInput" Width="150" Margin="5" />

            <Label Content="Description" VerticalAlignment="Center"/>
            <TextBox x:Name="DescriptionInput" Width="200" Margin="5"/>

            <Label Content="Due Date:" VerticalAlignment="Center"/>
            <DatePicker x:Name="DueDateInput" Width="120" Margin="5"/>

            <Button Content="Add" Margin="5" Click="AddTask_Click"/>

            <Button Content="Cancel" Margin="5" Click="CancelAdd_Click"/>
        </StackPanel>

        <!--Row 2: Action buttons-->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Add Task" Margin="5" Click="ShowAddPanel_Click"/>
            <Button Content="Edit Task" Margin="5" Click="EditTask_Click"/>
            <Button Content="Delete Task" Margin="5" Click="DeleteTask_Click"/>
            <Button Content="Mark Complete" Margin="5" Click="Mark_Complete_Click"/>
        </StackPanel>
    </Grid>
</Window>
